# Executables
allineate
allineate.baseline
/test_capi_nullopts
/test_qwarp_capi

# Object files
*.o
*.obj

# Debug-symbol bundles (from `make sanitize`/`make debug`)
*.dSYM/

# Large local scratch data (not test fixtures — use `git add -f` to track intentionally)
/MNI152_T1_0.5mm.nii.gz
/qwarp/
/weight/
/reface/
audit*.md

# macOS
.DS_Store

# Editor/IDE
*.swp
*.swo
*~

# Benchmark inputs/templates/masks are tracked (shareable). Only the regenerable
# registration outputs are ignored.
benchmark/outputs/

# Python cache
__pycache__/

# Stray root registration outputs (test artifacts; regenerable)
/*.nii.gz
/*.nii

# Reference papers (kept locally, not tracked)
flirt.pdf
